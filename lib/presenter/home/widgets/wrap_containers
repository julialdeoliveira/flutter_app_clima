class WrapContainer extends StatelessWidget {
    const WrapContainer({
      Key? key,
    }) : super(key: key);
  
    @override
    Widget build(BuildContext context) {
      return Wrap(
        children: const [
          ContainerLampHome(),
          ContainerLampHome(),
          ContainerLampHome(),
          ContainerLampHome(),
        ],
      );
    }
  }
  
  
  
  
  
  